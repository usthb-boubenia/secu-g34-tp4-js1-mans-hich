"use strict";  // utilisation du mode strict (slide 51)

let nom = "Doe";
let prenom = "John";
let age = 30;               // utilisation de let pour declarer les variables (slide 51)
let note = 15;
let pi = 3.14;


// utilisation des fonctions (slide 45)
// console.log pour afficher les valeurs (slide 33)
// utilisation des commentaires (slide 36)


// Fonction pour afficher le prénom
function f1() {
  console.log(prenom);
}

// Fonction pour afficher le nom
function f2() {
  console.log(nom);
}

// Fonction pour afficher nom et prénom concaténés
function f3() {
  console.log(prenom + "-" + nom);
}

// Fonction pour afficher l'âge
function f4() {
  console.log(age);
}

// Fonction pour afficher la note
function f5() {
  console.log(note);
}

// Fonction pour afficher la valeur de pi
function f6() {
  console.log(pi);
}
